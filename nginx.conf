events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    
    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
    
    # Basic Settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    
    # Gzip Settings
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;

    server {
        listen 80;
        server_name localhost;
        root /usr/share/nginx/html;
        index index.html;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
        add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline' 'unsafe-eval'" always;

        # Main dashboard route
        location = / {
            try_files /index.html =404;
        }

        # Favicon
        location = /favicon.ico {
            log_not_found off;
            access_log off;
            add_header Content-Type "image/x-icon";
            return 200 "";
        }
        
        # Dashboard assets
        location /dashboard-styles.css {
            try_files /dashboard-styles.css =404;
        }

        # RoboJar Analyzer routes
        location /robojar-analyzer/ {
            alias /usr/share/nginx/html/packages/robojar-analyzer/;
            try_files $uri $uri/ /packages/robojar-analyzer/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Dose Predictor routes
        location /dose-predictor/ {
            alias /usr/share/nginx/html/packages/dose-predictor/;
            try_files $uri $uri/ /packages/dose-predictor/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # MWAT/DDMAX Calculator routes
        location /mwat-ddmax/ {
            alias /usr/share/nginx/html/packages/mwat-ddmax/;
            try_files $uri $uri/ /packages/mwat-ddmax/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Regulation 100 Study Guide routes
        location /regulation-100-study/ {
            alias /usr/share/nginx/html/packages/regulation-100-study/;
            try_files $uri $uri/ /packages/regulation-100-study/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Water Treatment Flashcards routes
        location /water-treatment-flashcards/ {
            alias /usr/share/nginx/html/packages/water-treatment-flashcards/;
            try_files $uri $uri/ /packages/water-treatment-flashcards/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Sodium Hypochlorite Calculator routes
        location /sodium-hypochlorite-calculator/ {
            alias /usr/share/nginx/html/packages/sodium-hypochlorite-calculator/;
            try_files $uri $uri/ /packages/sodium-hypochlorite-calculator/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Data Parser routes
        location /data-parser/ {
            alias /usr/share/nginx/html/packages/data-parser/;
            try_files $uri $uri/ /packages/data-parser/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # Fan Press Tracker routes
        location /fan-press-tracker/ {
            alias /usr/share/nginx/html/packages/fan-press-tracker/;
            try_files $uri $uri/ /packages/fan-press-tracker/index.html;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }


        # All packages routes
        location /packages/ {
            try_files $uri $uri/ =404;
            
            location ~* \.(js|css|html)$ {
                expires 1h;
                add_header Cache-Control "public, immutable";
            }
        }

        # City logos
        location /city-logos/ {
            try_files $uri =404;
            expires 7d;
            add_header Cache-Control "public, immutable";
        }

        # Data files (optional, you might want to restrict this)
        location /data/ {
            try_files $uri =404;
            expires 1h;
        }

        # Static assets caching
        location ~* \.(ico|css|js|gif|jpe?g|png|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            try_files $uri =404;
        }

        # Handle 404s
        error_page 404 /404.html;
        location = /404.html {
            root /usr/share/nginx/html;
            internal;
        }

        # API proxy to Node.js server
        location /api/ {
            proxy_pass http://localhost:3001/api/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Health check endpoint
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }
    }
}